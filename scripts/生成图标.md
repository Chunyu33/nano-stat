### 生成图标
1. node scripts/generate-icons.js

2. node scripts/generate-ico.js

`generate-icons.js` 会从 `src-tauri/icons/icon.svg` 生成 Tauri/Windows Store 图标，
并同步 React 使用的 `public/icons/32x32.png`、`48x48.png` 和 `128x128.png`。

